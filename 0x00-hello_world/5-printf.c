#include <stdio.h>

/**
 * main - a room where programs work
 * printf: to output a text
 * Return: always 0 (that is return nothing)
 */
int main(void)
{
printf("with proper grammar,but the outcome is a piecie of art,\n");
return (0);
}
